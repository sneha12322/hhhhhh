# API Requests are handled by functions/api/[[path]].js to support POST methods

# Explicit Frontend Routes (Ensures Cloudflare serves the SPA)
/login /index.html 200
/dashboard /index.html 200
/links/* /index.html 200

# Proxy unknown paths (short link slugs) to Railway
/*  https://hhhhhh-production-8b24.up.railway.app/:splat  200